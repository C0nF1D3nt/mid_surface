# 任务说明：薄壁模型中面网格的面ID映射与全三角化

## 1. 背景与目标
目前我们正在处理一个薄壁模型的网格数据。前置工序中，我们在薄壁模型面网格中进行了“面对匹配”（匹配的侧面称为**匹配面**，其余为**侧面**）。侧面重新生成了内部不包含点的三角面网格，并在模型内部生成了四面体网格。通过连接四面体边的中点，我们提取出了一层包含三角形和四边形的混合网格，即**中面网格**。

**当前痛点**：提取出的中面网格丢失了原始面网格的**面ID（Face ID）**信息，即现在不知道中面上的每个网格单元来自哪两个匹配的面对。

**任务目标**：

1. 为中面网格找回并赋予面ID信息（每个三角形用一个 Face ID Pair 来代表它的面对关系索引）。
2. 将中面网格中的四边形全部转换为三角形，输出全三角形的中面网格。

## 2. 输入与输出
* **输入文件**：
  1. `surface_mesh.vtk`：原始带有面ID的表面网格。
  2. `volume_mesh.vtk`：内部生成的四面体体网格。
  3. `mid_surface.vtk`：初步生成的中面网格（包含三角形和四边形，缺失面ID）。
* **输出文件**：
  * `mid_surface_tri.vtk`：处理后的中面网格，要求全部为三角形单元，且每个单元的 Cell Data 或 Field Data 中包含名为 `surface_id`的信息。
* **技术栈要求**：C++

## 3. 算法处理逻辑

算法需分两步对中面网格的单元进行处理：

### 第一步：处理现有的中面三角形单元
中面上的每个三角形必定对应原四面体网格中的一个特定四面体。该四面体的4个顶点分布规律为：**3个点在某一个匹配面上（构成一个三角形），另1个点在另一个匹配面上。**

针对这四个点：
1. **获取第一个 Face ID**：包含 3 个顶点的那一侧，其构成的原始面网格三角形必然具有一个明确的面ID。以此作为索引 Pair 的第一个 ID。
2. **获取第二个 Face ID（单点侧）**：需要根据这唯一的第 4 个点在对侧匹配面上的位置来判定：
   * **情况 A（点在面内部）**：如果该点落在某个面的内部，直接提取该面的 ID 作为第二个 ID。
   * **情况 B（点在边界上）**：如果该点恰好在两个面的交界线上，需计算中面三角形在这两个面上的投影面积：
     * 取投影面积占中面三角形总面积比例**大于 70%** 的那个面，取其 ID。
     * 如果向两个面投影的面积占比均**不超过 70%**，则判定该面不与任何面匹配（属于 0-1 关系）。此时不取这俩面的 ID，将第二个 ID 标记为特殊值 `-1`。
3. **完成赋值**：经过此步，中面原有的所有三角形都获得了一个明确的面 ID 对。

### 第二步：处理现有的中面四边形单元（拆分与继承）
由于四边形的周围一定是第一步中已经处理并确定了 Face ID 的三角形，四边形的拆分策略及其产生的两个新三角形的面 ID 需由其邻居决定。

1. **判断拆分对角线**：检查四边形四个顶点周围关联的、已确定 ID 的三角形邻居。
   * **差异化拆分**：如果四边形中有两个对角顶点，它们各自周围的相邻三角形属于**不同的** Face ID 关系，则连接这两个点作为对角线，将四边形切分为两个三角形。
   * **任意拆分**：如果未发现上述差异（即周围相邻三角形的 ID 关系基本一致），则可任意连接一条对角线进行拆分。
2. **分配 Face ID**：切分后生成的两个新三角形，直接继承它们各自所在的局部区域（即各边所属的已有 ID 的三角形）的 Face ID 对。

### 第三步：根据面ID对得到surface_id

将Face ID对通过哈希的方式进行重排，每个Face ID对与一个`surface_id`进行对应，从0开始，并且赋给每一个三角单元作为属性。

## 4. 边界条件与异常处理
* 需确保投影计算和点-面位置关系（内部/边界）判断的几何容差（Tolerance）设置合理，避免浮点数精度问题导致的误判。
* 请确保最终输出的 VTK 拓扑结构正确（纯 Triangle 单元），并且 `surface_id` 数据类型挂载正确。
* 请编写清晰、模块化的代码，适当包含注释以便于后续调试和维护。